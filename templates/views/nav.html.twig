{% block nav %}
<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Fixed navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse"
      aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav me-auto mb-2 mb-md-0">
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('home') }}">Inicio</a> <hr>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('action-login') }}">Iniciar sesión</a> <hr>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('action-register') }}">Registro</a> <hr>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('action-logout') }}">Cerrar sesión</a> <hr>
          {# <li class="nav-item"><a class="nav-link fa-">prueba</a> <hr></li> #}
          <i class="fa-regular fa-home">holas</i>
      </ul>
    </div>
  </div>
</nav>
<style>
  .navbar-nav {
    display: flex;
    align-items: center;
    height: 100%; /* Opcional: para asegurarse de que la altura de la barra de navegación se ajuste correctamente */
  }

  .nav-item {
    display: flex;
    align-items: center;
  }

  .nav-link {
    margin: 0; /* Elimina el espacio adicional alrededor de los enlaces */
  }
</style>
{% endblock %}