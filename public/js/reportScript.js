$(function () {
    // $('form').submit(function(event) {
    //     event.preventDefault(); // Evita el envío del formulario por defecto

    //     // Obtiene los valores de los asistentes y los serializa a JSON
    //     var assistantsArray = [];
    //     $('input[name="assistants[]"]').each(function() {
    //         assistantsArray.push($(this).val());
    //     });
    //     var assistantsJson = JSON.stringify(assistantsArray);

    //     // Actualiza el valor del input oculto con el JSON serializado
    //     $('#assistants_json').val(assistantsJson);

    //     // Envía el formulario manualmente
    //     $(this).unbind('submit').submit();
    // });
})